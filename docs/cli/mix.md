# mix

Mix speech and noise at a target SNR.

## Usage

- `pixi run triton mix <speech-path> <noise-path> --snr-db -5 --output-dir outputs`

## Options

- `--snr-db`: target SNR in dB
- `--output-dir`: output directory
- `--sample-rate`: resample rate (optional)
